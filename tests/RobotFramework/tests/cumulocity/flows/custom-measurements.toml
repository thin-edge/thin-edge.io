input.mqtt.topics = ["custom/+/+/+/+/m/+"]

config = { device_id = "${mapper.device.id}" }

steps = [
    { script = "custom-measurements.js" },
    { builtin = "add-timestamp", config = { property = "time", format = "rfc3339", reformat = true } },
]