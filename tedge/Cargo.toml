[package]
name = "tedge"
version = "0.1.0"
edition = "2018"
readme = "README.md"

[dependencies]
anyhow = "1.0.38"
structopt = "0.3.21"
tokio = { version = "1.0", features = ["full"] }
mqtt_client = { path = "../mqtt/mqtt_client" }
futures = "0.3"
thiserror = "1.0"

[dev-dependencies]
assert_cmd = "1.0.2"
predicates = "1.0.5"
