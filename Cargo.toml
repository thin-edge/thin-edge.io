[workspace]

members = [
    "common/certificate",
    "common/mqtt_client",
    "tedge",
    "tedge_config",
    "mapper/tedge_mapper",
    "mapper/cumulocity/c8y_translator_lib",
    "mapper/thin_edge_json",
    "dm-agent"
]

[profile.release]
lto = true
