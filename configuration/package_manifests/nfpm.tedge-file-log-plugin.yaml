# yaml-language-server: $schema=https://nfpm.goreleaser.com/static/schema.json
---
name: tedge-file-log-plugin
description: |
  thin-edge.io plugin for file-based log management
arch: "${PKG_ARCH}"
platform: "linux"
version: "${GIT_SEMVER}"
release: "${RELEASE}"
section: misc
priority: "optional"
maintainer: "thin-edge.io team <info@thin-edge.io>"
vendor: "thin-edge.io"
homepage: "https://thin-edge.io"
license: "Apache-2.0"

depends:
  - tedge

deb:
  fields:
    Vcs-Browser: ${CI_PROJECT_URL}
    Vcs-Git: ${CI_PROJECT_URL}
  compression: xz

contents:
  # Symlink to log-plugins dir
  - src: /usr/bin/tedge-file-log-plugin
    dst: /etc/tedge/log-plugins/file
    type: symlink